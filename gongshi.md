# 关于公式是显示

 使用的框架和库

  1. 核心库：
    - remark-math: 解析Markdown中的数学公式语法
    - rehype-katex: 将解析后的数学公式渲染为KaTeX
    - katex: 实际的数学公式渲染引擎
  2. 集成位置：
    - 主要在components/markdown.tsx中配置和使用

  支持的公式语法

  1. 行内公式：
    - 新格式：$...$
    - 旧格式：\(...\)（会自动转换为新格式）
  2. 块级公式：
    - 新格式：$$...$$
    - 旧格式：\[...\]（会自动转换为新格式）

  公式处理流程

  1. 渲染流程：
    - 消息内容通过components/message.tsx传递给Markdown组件
    - Markdown组件使用remark-math解析公式语法
    - 使用rehype-katex将公式渲染为美观的数学表达式
  2. 数据存储转换：
    - 在保存到数据库前，通过convertLatexFormulas函数统一转换为新格式
    - 确保数据一致性

  这种实现方式能够很好地支持LaTeX数学公式，并且兼容新旧两种语法格式。


  现在让我创建一个任务列表来确保我们正确地支持旧格式的公式：

● 通过分析代码，我发现目前的实现存在一个问题：虽然数据库存储时会转换旧格式的公式（\(.
  ..\) 和 \[...\]），但在消息渲染时并没有进行同样的转换。这意味着如果数据库中已经存储
  了旧格式的公式或者有未经过转换的旧格式内容，它们可能不会正确显示。

  让我们解决这个问题，确保在消息渲染时也能正确处理旧格式的公式。

  首先，我需要修改消息组件，在渲染Markdown之前进行公式格式转换：


# LaTeX 公式多次转义问题

## 问题描述
在流式处理 AI 返回的消息时，LaTeX 公式标记被多次转义，导致显示错误。

## 问题示例

### 示例 1：LaTeX 行内公式标记
```
正确格式：  \(x + y\)
实际收到：  \\(x + y\\)
或者：      \\\\(x + y\\\\)
```

### 示例 2：LaTeX 命令
```
正确格式：  \angle ABC
实际收到：  \\angle ABC
或者：      \\\\angle ABC
```

### 示例 3：完整的数学表达式
```
正确格式：  \(\angle OAD = 15^{\circ}\)
实际收到：  \\(\\angle OAD = 15^{\\circ}\\)
```

### 示例 4：三角形符号
```
正确格式：  \triangle ABC
实际收到：  \\triangle ABC
```

### 示例 5：实际日志片段
```
"0:\"从题目看，已知正方形$ABCD$内有个点$O$，\\\\(\\\\angle "
"0:\"OAD=\\\\angle "
"0:\"ODA "
"0:\"= "
"0:\"15^{\\\\circ}\\\\)，要证\\\\(\\\\triangle "
```

## 根本原因
消息在传输/序列化过程中被多次 JSON 编码，每次编码都会对反斜杠进行转义：
- 第 1 次：`\` → `\\`
- 第 2 次：`\\` → `\\\\`
- 第 3 次：`\\\\` → `\\\\\\\\`

## 期望效果
无论收到几次转义的内容，都能正确还原并转换为标准的 Markdown 数学公式格式：
```
\\(\\angle ABC\\)  →  $\angle ABC$
\\\\(\\\\angle ABC\\\\)  →  $\angle ABC$
```